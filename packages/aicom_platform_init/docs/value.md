# aicom_platform_init — Value

## English

Bootstraps local SQLite on desktop so finance and audit apps store data on-device reliably — small helper, big privacy win.

## Русский

Поднимает локальный SQLite на desktop, чтобы финансовые и audit-приложения надёжно хранили данные на устройстве — маленький пакет, большой выигрыш в приватности.

---

*Regenerate: `python3 scripts/bootstrap_product_value.py`*
